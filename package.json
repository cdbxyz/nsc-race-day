{
  "name": "nsc-race-day",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Officer of the Day race management PWA for Netley Sailing Club.",
  "comment": "Node is only ever used to run the tests. Nothing here is built, bundled or shipped — GitHub Pages serves the repo as-is.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
